{% extends 'base.html.twig' %}

{% block title %}New Todo{% endblock %}


    {% block body %}
        <div class="container">
            <h1>Create new Todo</h1>
        
            {{ include('todo/_form.html.twig') }}
        
            <a class="btn btn-primary" href="{{ path('app_todo_index') }}">Back to list</a>
        </div>
    {% endblock %}

